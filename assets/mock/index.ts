/**
 * Mock media assets for development.
 * Used by getMediaSource.ts when USE_LOCAL_MEDIA = true.
 * Swap these files out for higher-quality placeholders at any time.
 */

export const mockVideo = require('./mock-video.mp4');
export const mockImage = require('./mock-image.jpg');
export const mockAvatar = require('./mock-avatar.jpg');
export const mockAudio = require('./mock-audio.m4a');
